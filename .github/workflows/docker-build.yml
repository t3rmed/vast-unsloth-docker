name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    paths:
      - 'Dockerfile'

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 Checkout repository
      uses: actions/checkout@v4

    # CRITICAL FIX: Run the cleanup script to free disk space before the build starts (prevents I/O errors)
    - name: 🧹 Free Disk Space (28+ GB)
      run: bash free_disk_space.sh
      shell: bash

    - name: 🐳 Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: 🔑 Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        # Use your custom secret names for authentication
        username: ${{ secrets.DOCKER_USER }} 
        password: ${{ secrets.DOCKER_PAT }}

    - name: 🏗️ Build and Push to Docker Hub
      uses: docker/build-push-action@v6
      with:
        context: .
        push: true
        tags: ${{ secrets.DOCKER_USER }}/unsloth-custom:latest
        platforms: linux/amd64
